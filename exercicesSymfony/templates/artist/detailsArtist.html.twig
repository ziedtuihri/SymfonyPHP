    {% extends 'base.html.twig' %}

    {% block body %}
    
    <h1> artist</h1>
    <table border=2>
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Biography</th>
            </tr>
        </thead>
        <tbody>
                <tr>
                    <td>{{ artist.id }}</td>
                    <td>{{ artist.name }}</td>
                    <td>{{ artist.biography }}</td>
                </tr>
        </tbody>
    </table>

    {% endblock %}